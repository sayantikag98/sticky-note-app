<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head.ejs") %> 
<body>
    <header id = "notes-header">
        <div id = "notes-title-div">
            <h1>Sticky Notes</h1>
        </div>
        <div id = "notes-link-div">
            <h4>
                <a id = "home-link" title = "Home" href = "/">🏠</a>      
            </h4>
            <h4>
                <a id = "about-link" title = "About" href = "/about">🔍</a>
            </h4>
            <h4>
                <a id = "create-link" title = "New Note" href = "/notes/create">📝</a>
            </h4>
        </div>
    </header>
    <main id = "notes-main">
        <div id = "notes-search-content">
            <input id = "notes-input" placeholder = "Search note title here" autofocus type = "text"/>
            <i id = "notes-search-icon" class = "material-icons">search</i>
        </div>
        <div id = "notes-main-content">
            <% if(!notes.length){ %> 
                <h3>No notes to display...</h3>
            <% }else{ %> 
                <%notes.forEach(note => { %>
                    <% const notePath = `/notes/${note._id}` %> 
                    <h1 class = "notes-h1"><a class = "notes-a" title = "Go to this note"  href = <%= notePath %> ><%= note.title %> </a></h1>
                <% }) %> 
            <% } %> 
        </div> 
    </main>
    <%- include("../partials/footer.ejs") %> 
    <script src = "/js/search.js" data-notes = '<%= JSON.stringify(notes) %>' ></script>
    <!-- in order to pass parameters in script tag first stringify the parameter value
    and then parse while receiving the value in javascript file -->
</body>
</html>